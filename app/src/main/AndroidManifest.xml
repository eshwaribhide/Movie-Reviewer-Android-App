<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.madcourse.numad22sp_moviereviewer_teamdedj">
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_movie_review_dedj"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_movie_review_dedj_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD22Sp_MovieReviewer_TeamDEDJ">
        <activity
            android:name=".StaticProfilePageActivity"
            android:label="Profile"
            android:parentActivityName=".NavigationActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".NavigationActivity" />
        </activity>
        <activity
            android:name=".SearchUsersActivity"
            android:label="Search Users"
            android:parentActivityName=".NavigationActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".NavigationActivity" />
        </activity>
        <activity
            android:name=".MovieDetailsActivity"
            android:label="About This Movie"
            android:parentActivityName=".NavigationActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".NavigationActivity" />
        </activity>
        <activity
            android:name=".NavigationActivity"
            android:label="Menu"
            android:parentActivityName=".MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".HomePage"
            android:label="My Feed"
            android:parentActivityName=".NavigationActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".NavigationActivity" />
        </activity>
        <activity
            android:name=".MovieListActivity"
            android:label="Search For Movies"
            android:parentActivityName=".NavigationActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".NavigationActivity" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Movie Review App"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".leaderboard.LeaderboardActivity"
            android:label="Leaderboard"
            android:parentActivityName=".NavigationActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".NavigationActivity" />
        </activity>
        <activity
            android:name=".profilePageV2.ProfilePageV2Activity"
            android:label="Profile"
            android:parentActivityName="NavigationActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="NavigationActivity" />
        </activity>
        <activity
            android:name=".theater.TheaterNearMeActivity"
            android:label="Theaters Near Me"
            android:parentActivityName="NavigationActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="NavigationActivity" />
        </activity>
    </application>

</manifest>